export default interface ProfileBase {
    firstName:string
    birthDate:Date
    sex:string
    target:string
    intimacy:string
    photos: string[]
    interests:string[]
    socials:string[]
    bio:string
}