export default interface ProfileBase {
    firstName:string
    localization:string
    birthDate:Date
    sex:string
    target:string
    intimacy:string
    photos: string[]
    interests:string[]
    socials:string[]
    bio:string
}